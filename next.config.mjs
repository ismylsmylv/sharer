/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['cdn2.steamgriddb.com', 'themoviedb.org', 'play-lh.googleusercontent.com'],
    },
};

export default nextConfig;
